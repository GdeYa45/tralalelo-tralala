# HTTP tests (Этап 8.4)

Файлы лежат в папке `http/` и предназначены для запуска в **IntelliJ IDEA / JetBrains HTTP Client** (формат `.http`).

## Как прогнать

1) Запустить проект через Docker:

```bash
docker compose up -d --build